"""
"""

def connectPointToLine(point, linePoints, tifData):
    """
    Find the brightest path between a single point and a line of points.

    Limit the search to a range of points around the nearest point between point and linePoints.

    Args:
        point: (x,y,z) coordinates of the point
        linePoints: List of points along the line
        tifData: np.ndarray of intensity (slice, x, y)

    Returns:
        The point number (within linePoints that is the brightest path.
    """

    # TODO (Cudmore) implement this !!!
    return None